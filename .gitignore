node_modules/
.next/
out/
build/
.npm-cache/

.env
.env*.local
.env*

.vercel
*.tsbuildinfo
.DS_Store
npm-debug.log*

# Must stay LAST. The root .gitignore has a bare `.env*`, and `vercel link`
# appends another one above; a negation only wins if nothing re-ignores it
# afterwards. This template holds no secrets and must be committed.
!.env.example
